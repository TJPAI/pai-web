name: Unify swipe menu scroll v43
on:
  push:
    paths:
      - 'tools/unify_swipe_menu_scroll_v43.py'
jobs:
  apply:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - name: Apply patch
        run: python tools/unify_swipe_menu_scroll_v43.py
      - name: Validate
        run: |
          python tools/validate.py
          node --check assets/js/site.js
      - name: Commit and self-clean
        run: |
          rm tools/unify_swipe_menu_scroll_v43.py .github/workflows/unify-swipe-menu-scroll-v43.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'fix: unify menu and swipe destination scroll'
          git push
