name: Fix swipe footer clip v35
on:
  push:
    paths:
      - 'tools/fix_swipe_footer_clip_v35.py'
      - '.github/workflows/fix-swipe-footer-clip-v35.yml'
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - name: Apply fix
        run: python tools/fix_swipe_footer_clip_v35.py
      - name: Validate
        run: |
          python tools/validate.py
          node --check assets/js/site.js
      - name: Commit and self-clean
        run: |
          rm tools/fix_swipe_footer_clip_v35.py .github/workflows/fix-swipe-footer-clip-v35.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'fix: clip swipe preview above visible footer'
          git push
