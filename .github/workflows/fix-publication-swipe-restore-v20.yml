name: Fix publication swipe restore v20

on:
  push:
    branches: [main]
    paths:
      - 'tools/fix_publication_swipe_restore_v20.py'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - name: Apply patch
        run: python tools/fix_publication_swipe_restore_v20.py
      - name: Validate
        run: |
          python tools/validate.py
          node --check assets/js/site.js
      - name: Commit and self-clean
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm tools/fix_publication_swipe_restore_v20.py .github/workflows/fix-publication-swipe-restore-v20.yml
          git add assets/js/site.js '*.html' 'en/*.html' 'people/*.html' 'en/people/*.html' || true
          git commit -m "fix: preserve publication position across swipe navigation"
          git push
