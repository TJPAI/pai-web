name: Migrate support/meta typography
on:
  push:
    branches: [main]
    paths:
      - 'tools/migrate_support_meta_typography.py'
      - '.github/workflows/migrate-support-meta-typography.yml'
permissions:
  contents: write
jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - name: Apply migration
        run: python tools/migrate_support_meta_typography.py
      - name: Validate site
        run: python tools/validate.py
      - name: Commit migration and self-clean
        run: |
          rm tools/migrate_support_meta_typography.py .github/workflows/migrate-support-meta-typography.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add assets/css/site.css assets/css/refine-base.css tools/migrate_support_meta_typography.py .github/workflows/migrate-support-meta-typography.yml
          git commit -m 'refactor: route support and meta text through typography tokens'
          git push
